{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && npm ci && npx vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "@clerk/backend": "^1.27.0",
    "encore.dev": "^1.50.6"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}